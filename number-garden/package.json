{
  "name": "number-garden",
  "version": "3.1.0+7139b55",
  "private": true,
  "description": "A mobile-first place-value addition, subtraction, and multiplication game.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html",
    "validate:android-release-tracker": "node scripts/validate-android-release-tracker.mjs --check-git --print-ready",
    "test:android-release-tracker": "node tests/android-release-tracker.test.mjs",
    "test:android-release-docs": "node scripts/check-android-release-docs.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
