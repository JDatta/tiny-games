{
  "name": "number-garden",
  "version": "3.0.0+b868ee6",
  "private": true,
  "description": "A mobile-first place-value addition, subtraction, and multiplication game.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
